// node_modules/thirdweb/dist/esm/utils/type-guards.js
function isObject(value) {
  return typeof value === "object" && value !== null;
}
function isString(value) {
  return typeof value === "string";
}
function isObjectWithKeys(value, keys = []) {
  return isObject(value) && keys.every((key) => key in value);
}
function isRecord(value, guards) {
  const keyGuard = (guards == null ? void 0 : guards.key) ?? isString;
  const valueGuard = (guards == null ? void 0 : guards.value) ?? isString;
  return isObject(value) && !Array.isArray(value) && Object.entries(value).every(([k, v]) => keyGuard(k) && valueGuard(v));
}

export {
  isString,
  isObjectWithKeys,
  isRecord
};
//# sourceMappingURL=chunk-JJI46KRT.js.map
